<h1><%= title %></h1>

<a href="/rumors" class="btn">← กลับไปข่าวลือทั้งหมด</a>

<p style="margin: 20px 0; color: #dc3545; font-weight: bold;">
    ข่าวลือที่มีจำนวนรายงานเกินค่าที่กำหนด (≥5 รายงาน) จะแสดงที่นี่
</p>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>หัวข้อข่าวลือ</th>
            <th>แหล่งที่มา</th>
            <th>วันที่สร้าง</th>
            <th>จำนวนรายงาน</th>
            <th>สถานะ</th>
            <th>การกระทำ</th>
        </tr>
    </thead>
    <tbody>
        <% if (rumors.length === 0) { %>
            <tr>
                <td colspan="7" style="text-align: center;">ไม่มีข่าวลือสถานะ Panic</td>
            </tr>
        <% } else { %>
            <% rumors.forEach(rumor => { %>
                <tr>
                    <td><%= rumor.Rumour_ID %></td>
                    <td><%= rumor.Rumour_Title %></td>
                    <td><%= rumor.Source %></td>
                    <td><%= new Date(rumor.Create_Date).toLocaleDateString('th-TH') %></td>
                    <td><strong><%= rumor.report_count %></strong></td>
                    <td>
                        <span class="badge badge-panic">PANIC</span>
                        <% if (rumor.Is_Verified) { %>
                            <span class="badge badge-verified">VERIFIED</span>
                        <% } %>
                    </td>
                    <td>
                        <a href="/rumors/<%= rumor.Rumour_ID %>" class="btn btn-primary">ดูรายละเอียด</a>
                    </td>
                </tr>
            <% }) %>
        <% } %>
    </tbody>
</table>
