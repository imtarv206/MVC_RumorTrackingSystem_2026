<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - ระบบติดตามข่าวลือ</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        nav {
            background: #333;
            color: #fff;
            padding: 15px 0;
            margin-bottom: 20px;
        }
        nav .container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        nav a {
            color: #fff;
            text-decoration: none;
            padding: 10px 15px;
            margin: 0 5px;
        }
        nav a:hover {
            background: #555;
        }
        .content {
            background: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        h1 {
            color: #333;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        table th, table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        table th {
            background-color: #333;
            color: white;
        }
        table tr:hover {
            background-color: #f5f5f5;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background: #333;
            color: #fff;
            text-decoration: none;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            margin: 5px;
        }
        .btn:hover {
            background: #555;
        }
        .btn-primary {
            background: #007bff;
        }
        .btn-primary:hover {
            background: #0056b3;
        }
        .btn-danger {
            background: #dc3545;
        }
        .btn-danger:hover {
            background: #c82333;
        }
        .btn-success {
            background: #28a745;
        }
        .btn-success:hover {
            background: #218838;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 3px;
        }
        .badge {
            padding: 5px 10px;
            border-radius: 3px;
            color: #fff;
            font-size: 12px;
        }
        .badge-normal {
            background: #28a745;
        }
        .badge-panic {
            background: #dc3545;
        }
        .badge-verified {
            background: #007bff;
        }
    </style>
</head>
<body>
    <nav>
        <div class="container">
            <div>
                <a href="/">หน้าแรก</a>
                <a href="/rumors">ข่าวลือทั้งหมด</a>
                <a href="/rumors/panic">ข่าวลือ Panic</a>
                <a href="/reports">รายงานทั้งหมด</a>
                <a href="/users">ผู้ใช้งาน</a>
            </div>
            <div>
                <a href="/rumors/create">+ สร้างข่าวลือ</a>
                <a href="/reports/create">+ สร้างรายงาน</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="content">
            <%- body %>
        </div>
    </div>
</body>
</html>
